.venv/
__pycache__/
*.pyc
web/node_modules/
web/dist/
web/data/
web/test-results/

# Full-corpus predeploy output (~6000 per-Act .json/.xml). The 4 minimal E2E
# fixture files under web/public/data/ are already tracked and stay tracked;
# this only keeps the generated corpus out of `git add -A`.
web/public/data/
*.egg-info/

# Local Netlify folder
.netlify

# Atomic-write scratch file from lex-au-explorer-verify
web/verification.json.tmp
